# via git bash
cd ActiveLBLM/
git checkout master # make sure you are on the master branch
git add .
git commit -m "test_push"
git push --set-upstream origin master


# the static page can be run by chrome. 